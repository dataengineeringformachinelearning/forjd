# Thin wrapper so Fly GitHub / remote builders have a Dockerfile.
# Official images publish to docker.dragonflydb.io (Docker Hub is stale — last tag ~v1.27).
FROM docker.dragonflydb.io/dragonflydb/dragonfly:v1.39.0
