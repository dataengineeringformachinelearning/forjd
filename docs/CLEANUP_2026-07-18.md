# Final cleanup pass — 2026-07-18

Boundary enforced: FORJD is universal and partner-agnostic. Partner product names,
BFF flags, and Railway/Vercel ops live in the partner repository.

## Removed / generalized

| Item | Action |
|------|--------|
| `docs/PRODUCTION_CUTOVER_CHECKLIST.md` DEML Fly/Vercel/Railway sections | Rewritten FORJD-only; partner checklist referenced externally |
| `scripts/pg_migrate_neon_to_supabase.sh` `MIGRATE_MODE=deml` default | Generalized to `partner` + required `TARGET_SCHEMA` (`deml` kept as alias) |
| `verify_supabase_post_migration.py` hardcoded `schema_deml_optional` | Optional `PARTNER_CONTROL_SCHEMA` env only |
| `scripts/verify_supabase_post_migration.sql` `deml` schema probe | Removed hardcoded partner schema |
| `docs/NEON_TO_SUPABASE.md` DEML Neon project specifics | Generic partner co-location runbook |
| `CUTOVER.md` production gate naming DEML phase flag | Generic “partner BFF steady state” |
| `ARCHITECTURE.md` SQL range | Updated `003` → `018` |
| `README.md` Postgres consolidation blurb | Partner-schema wording |

## Confirmed clean (no changes needed)

| Area | Status |
|------|--------|
| `backend/app/` / `engine/` | No `deml` / `X-DEML` / `deml-compat` hardcoding |
| Dep manifests | No Redpanda / ClickHouse / Kafka |
| Workflows | Universal YAML; no first-party `*deml*` workflow files |
| Auth | Tenant-bound `fjsvc_` only; no Firebase-as-FORJD-auth |

## Partner docs stay external

Example partner runbooks (DEML): cutover flags, Fly `deml-backend`, Vercel `deml`,
Railway retirement scripts — in the partner repo, not FORJD.
