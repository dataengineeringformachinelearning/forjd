# App
PROJECT_NAME=forjd
PROJECT_VERSION=0.1.0
ENVIRONMENT=development
DEBUG=true
HOST=0.0.0.0
PORT=8000

# CORS (JSON list) — include Angular default :4200
CORS_ORIGINS=["http://localhost:4200","http://127.0.0.1:4200","http://localhost:3000","http://localhost:5173","http://127.0.0.1:3000","http://127.0.0.1:5173"]

# Database — Supabase Postgres (Transaction / Session pooler DSN)
# Format: postgresql+asyncpg://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# Or direct: postgresql+asyncpg://postgres:[password]@db.[ref].supabase.co:5432/postgres
POSTGRES_DSN=postgresql+asyncpg://postgres:postgres@localhost:5432/forjd

# Cache — local Compose Dragonfly, or Fly private URL after deploy
# Fly example: redis://:PASSWORD@forjd-dragonfly.internal:6379/0
REDIS_URL=redis://localhost:6379/0

# Prefect
PREFECT_API_URL=http://127.0.0.1:4200/api

# Rollbar (post_server_item token). Empty = disabled.
ROLLBAR_ACCESS_TOKEN=
